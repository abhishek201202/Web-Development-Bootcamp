const add = function(x, y){
    return x + y;
}

const add2 = (x, y) => {
    return x + y;
}

const square = (x) => {
    return x * x;
}
const square2 = x => {
    return x * x;
}

const rollDie = () => {
    return Math.floor(Math.random() * 6) + 1;
}

